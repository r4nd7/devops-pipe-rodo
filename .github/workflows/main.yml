name: Java CI feature-pipe-rodo

on: 
  workflow_call:
   
jobs:

  build:
    runs-on: ubuntu-latest
    #runs-on: self-hosted
    steps:
 
    - name: Chechout
      uses: actions/checkout@v3.5.3
     
    - name: Setup Java JDK
      uses: actions/setup-java@v3.12.0
      with:
       java-version: '11'
       distribution: 'zulu'
       
    - name: compile Gradle
      run: |
       chmod +x gradlew
       ./gradlew build
